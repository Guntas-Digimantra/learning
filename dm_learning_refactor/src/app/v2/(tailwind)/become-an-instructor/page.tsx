import BecomeInstructor from "@/components/become-instructor";

export const metadata = {
    title: "Become an Instructor | DMLearning",
    description: "Join DMLearning as an instructor and share your expertise with students. Apply now with your experience and resume.",
};

const page = () => {
    return <BecomeInstructor />;
};

export default page;
