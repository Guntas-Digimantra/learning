import MernStackComponent from "@/components/dummy"

export const metadata = {
  robots: { index: false, follow: true },
}

const page = () => {
   return (
         <MernStackComponent />
   )
}

export default page
